import SwiftUI

struct RingView: View {
    var body: some View {
        NavigationStack {
            Text("Ring")
                .navigationTitle("Ring")
        }
    }
}

#Preview {
    RingView()
}
